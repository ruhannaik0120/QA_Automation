"""Reusable, AI-agnostic helpers for ticket-scoped QA automation.

Modules in this package initialize ticket workspaces, acquire declared inputs,
and export approved evidence. Client-specific procedure belongs in workflows,
while database execution remains behind the separate MCP subsystem.
"""
