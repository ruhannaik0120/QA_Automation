"""PostgreSQL backend for the shared MCP database connector contract.

The package isolates PostgreSQL driver options and statement behavior from the
transport-facing MCP tools and service layer.
"""
